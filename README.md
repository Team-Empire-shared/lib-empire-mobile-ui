# Empire Mobile UI

Shared Expo/React Native component library for all Empire mobile apps.
Provides: auth screens, API client, push notifications, theme system.

## Setup
```bash
npm install
npm run build
```
